---
description: Quick scan — stack table, build/typecheck/tests/vulnerability check and a one-page first impression in taste.md (no reviewer agents)
argument-hint: [optional: "no-install" to skip dependency installation]
---

Load the `vibe-cook-audit:quick-scan` skill with the Skill tool and follow it for the project in the current working directory.

Arguments from the user: $ARGUMENTS

If the arguments include `no-install`, skip dependency installation and record the checks that need it as "could not run: no-install". Write `taste.md` in the project root and suggest `/cook` for the full audit.
